Class library, Common Language Runtime , Dynamic Language Runtime, Application Domains, Cross-language interoperability, Runtime Hosts, Framework security.
It contains assembly Version, Security identity,assembly scope, To resolve references to resources and classes.
It is part of the  security mechanism that prevents unauthorized access of resources and operations, and restricts the managed code to perform particular tasks and operations.
All .NET source code is compiled to Intermediate Language. IL is then converted to machine code by a Just-In-Time compiler.
It is a service for storing  messages that can be accessed from anywhere via authenticated calls using HTTP or HTTPS.



Intermediate language (IL) is an object-oriented programming language designed to be used by compilers for the .NET Framework before static or dynamic compilation to machine code.

Azure Queue storage is a service for storing large numbers of messages that can be accessed from anywhere in the world via authenticated calls using HTTP or HTTPS. 



qnakeyphrases [ 'Dynamic Language Runtime',
  'Common Language Runtime',
  'Runtime Hosts',
  'Cross-language interoperability',
  'Application Domains' ]
Common Language Runtime, Common Type System, Class library, Cross-language interoperability, Dynamic Language Runtime, Common Language Specification and Runtime Hosts
0.8

qnakeyphrases [ 'Version of assembly',
  'Security identity',
  'Scope',
  'references' ]
  Manifest contains all the metadata needed to specify the Version of assembly  and security identity and all metadata needed to define the scope of the assembly
0.75

qnakeyphrases [ 'operations',
  'unauthorized access of resources',
  'managed code',
  'particular tasks',
  'security mechanism' ]
  It  is a security mechanism by which the common language runtime can restrict the managed code to execute operations with a limited set of permissions.
  0.6


qnakeyphrases [ 'machine code',
  '.NET source code',
  'Intermediate Language',
  'Time compiler' ]
Intermediate language (IL) is an object-oriented programming language designed to be used by compilers for the .NET Framework before static or dynamic compilation to machine code
0.5

qnakeyphrases [ 'authenticated calls',
  'HTTPS',
  'service',
  'large numbers of messages' ]
  
  Azure Queue storage is a service for storing large numbers of messages that can be accessed from 
  anywhere in the world via authenticated calls using HTTP or HTTPS. 

  It is a service for storing large numbers of messages that can be accessed from anywhere via authenticated calls using HTTP or HTTPS.
  1
  


SPANISH

Common Language Runtime, biblioteca de clases, sistema de tipos común, interoperabilidad entre lenguajes, Dynamic Language Runtime, Common Language Specification y Hosts de tiempo de ejecución

Contiene versión de ensamblaje, identidad de seguridad, alcance de ensamblaje, para resolver referencias a recursos y clases.

Es un mecanismo de seguridad que common language runtime puede restringir el código administrado para ejecutar operaciones con un conjunto limitado de permisos.

Todo el código fuente de .NET se compila en lenguaje intermedio. IL se convierte a código de máquina mediante un compilador Just-In-Time.

Almacenamiento de cola azul es un servicio para almacenar gran cantidad de mensajes que pueden acceder desde cualquier lugar del mundo vía llamadas autenticadas mediante HTTP o HTTPS.
   
   Es un servicio para almacenar mensajes a los que se puede acceder desde cualquier lugar a través de llamadas autenticadas usando HTTP o HTTPS.


Feedback: Buena entrevista


GERMAN

Common Language Runtime, allgemeines Typsystem, Klassenbibliothek, sprachübergreifende Interoperabilität, Dynamic Language Runtime, Common Language Specification und Runtime Hosts

Es enthält die Version der Assembly, Sicherheitsidentität, Umfang der Versammlung, um Verweise auf Klassen und Ressourcen zu lösen.

Es ist ein Mechanismus für die gemeinsame Sprachlaufzeit Sicherheit den verwalteten Code, um Operationen mit einem begrenzten Satz von Berechtigungen einschränken kann.

Intermediate Language (IL) ist eine objektorientierte Programmiersprache, die von Compilern für .NET Framework vor der statischen oder dynamischen Kompilierung in Maschinencode verwendet werden soll

Blaue Schweif Lagerung ist ein Dienst für die Speicherung der großen Menge der Nachrichten, die von überall auf der Welt über authentifizierte Aufrufe von HTTP oder HTTPS zugegriffen werden kann.
   
   Es ist ein Dienst zum Speichern großer Mengen an Nachrichten, die von überall über authentifizierte Aufrufe über HTTP oder HTTPS zugegriffen werden können.

Feedback: Gutes Interview


FRENCH

Système de Type commun, Common Language Runtime, l’interopérabilité interlangage, Dynamic Language Runtime, Common Language Specification et hôtes de Runtime, bibliothèque de classes

Manifeste contient toutes les métadonnées nécessaires pour spécifier la Version de l’assembly et l’identité de sécurité et toutes les métadonnées nécessaires pour définir la portée de l’Assemblée

C’est un mécanisme par lequel le common language runtime peut restreindre le code managé pour exécuter des opérations avec un jeu limité d’autorisations de sécurité.

IL (intermediate language) est un langage de programmation orienté objet conçu pour être utilisé par les compilateurs pour le .NET Framework avant la compilation statique ou dynamique de code machine

Il s’agit d’un service pour stocker un grand nombre de messages qui sont accessibles depuis n’importe où via des appels authentifiés à l’aide de HTTP ou HTTPS.




ITALIAN

Common Language Runtime, Common Type System, libreria di classi, l'interoperabilità tra più linguaggi, Dynamic Language Runtime, Common Language Specification e host di Runtime

Manifesto contiene tutti i metadati necessari per specificare la versione di assembly e l'identità di sicurezza e tutti i metadati necessari per definire l'ambito dell'assembly 

È un meccanismo di sicurezza che common language runtime consente di limitare il codice gestito per eseguire operazioni con un set limitato di autorizzazioni. 

Microsoft intermediate language (IL) è un linguaggio di programmazione orientato agli oggetti progettato per essere utilizzato dai compilatori per .NET Framework prima della compilazione statica o dinamica in codice macchina  

È un servizio per la memorizzazione di un numero elevato di messaggi che è possibile accedere da qualsiasi tramite chiamate autenticate tramite HTTP o HTTPS.
